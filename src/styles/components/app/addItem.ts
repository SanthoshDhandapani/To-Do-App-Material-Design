import { createStyles } from '@material-ui/core';

export const style = createStyles({
  layout: {
    paddingTop: 10,
    paddingBottom: 10,
  },
  input: {
    paddingRight: 16,
  },
  addBtn: {
    textAlign: 'center',
  },
});
